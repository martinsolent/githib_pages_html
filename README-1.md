# 🎉 It worked. Your first GitHub Page is live.

You uploaded a file, switched on Pages, and the internet did the rest.  
That's it. That's web publishing.

---

## How you got here

### 1 — Create a repo
You made a new repository on GitHub and named it something sensible. That's your project's home.

### 2 — Upload your file
You added a file to the repo. GitHub looks for `index.html` by default — that's the page it serves.

### 3 — Switch on Pages
**Settings → Pages → Source: `main` branch.**  
GitHub builds and deploys your site automatically.

### 4 — Get your URL
GitHub gives you a public URL:

```
https://username.github.io/your-repo
```

Share it. It works on any device.

---

## Upload files in the browser

You don't need Git or a terminal to get files into GitHub.  
You can drag and drop straight from your desktop.

**Steps:**

1. Open your repo on GitHub
2. Click **Add file** → **Upload files**
3. Drag your file into the dashed upload box
4. Scroll down, write a short commit message — something like `add my page`
5. Click **Commit changes**

GitHub Pages picks up the change within about a minute.

> 💡 **Replacing a file?** Upload a new version with the exact same filename. GitHub overwrites the old one and your live site updates automatically.

---

## What this file is

This is a **Markdown** file — saved as `README.md`.

GitHub renders it automatically as a formatted page. You're looking at the result right now.

Markdown uses plain text symbols to add formatting:

| You type | You get |
|---|---|
| `# Heading` | Big heading |
| `## Heading` | Smaller heading |
| `**bold**` | **bold** |
| `*italic*` | *italic* |
| `` `code` `` | `code` |
| `- item` | bullet list |
| `1. item` | numbered list |
| `> quote` | blockquote |
| `[text](url)` | link |

### Example list

- No servers
- No hosting fees
- No nonsense

### Example code block

```html
<!DOCTYPE html>
<html>
  <head>
    <title>My Page</title>
  </head>
  <body>
    <h1>Hello, world.</h1>
  </body>
</html>
```

### Example blockquote

> Edit the file, commit the change, and your live site updates.  
> Every commit is a saved version of your work.

---

## What happens next

- Edit this file and commit — watch the page update live
- Every change you commit is saved in your repo's history
- You can always roll back to an earlier version

---

*Made with Markdown · Hosted free on GitHub Pages*
